spring:
  profiles:
    active: team

pagehelper:
  helper-dialect: mysql

server:
  port: 80
mybatis:
  type-handlers-package: com.cskaoyan.mall.admin.mybatis



logging:
  level:
    com:
      cskaoyan:
        mall:
          admin:
            mapper: debug
